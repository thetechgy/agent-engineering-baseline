{
  "skill_name": "podman",
  "evals": [
    {
      "id": 1,
      "prompt": "Deploy a new Redis service on this Fedora host with Podman. It needs to survive reboot and only my application containers should reach it.",
      "expected_output": "A rootless Quadlet/systemd design using an explicit private network, durable storage, meaningful health semantics, and pre-deployment validation without publishing Redis broadly.",
      "assertions": [
        "Uses rootless Quadlet and systemd rather than a persistent podman run command or podman generate systemd",
        "Uses an explicit private network and does not publish Redis to all host interfaces",
        "Evaluates persistent storage and a meaningful health strategy",
        "Includes Quadlet generation validation before live start",
        "Does not introduce CNI or slirp4netns guidance"
      ]
    },
    {
      "id": 2,
      "prompt": "The vendor only gives me evals/files/vendor-compose.yaml. I want this as a normal persistent Podman service on my server.",
      "expected_output": "A review-first Compose-to-Quadlet migration that treats the supplied file as untrusted source material and maps its services, networks, storage, health, secrets, and exposure semantically.",
      "files": [
        "evals/files/vendor-compose.yaml",
        "evals/files/vendor-web/Containerfile"
      ],
      "assertions": [
        "Reviews the Compose file and referenced build context before executing it",
        "Recognizes podman compose as an external-provider wrapper",
        "Designs native Quadlets rather than mechanically transliterating YAML",
        "Identifies the database as private and avoids publishing its port",
        "Does not expose the Podman socket to the application"
      ]
    },
    {
      "id": 3,
      "prompt": "Make my rootless reverse proxy listen directly on host ports 80 and 443.",
      "expected_output": "A rootless-first ingress recommendation that treats low ports as host policy and evaluates the existing ingress, redirection, or deliberate sysctl options.",
      "assertions": [
        "Does not switch the whole stack to rootful automatically",
        "Explains the low-port host-policy boundary",
        "Calls out the host-wide effect before recommending ip_unprivileged_port_start changes",
        "Keeps unrelated application containers rootless"
      ]
    },
    {
      "id": 4,
      "prompt": "Configure Traefik to discover all of my Podman containers automatically by mounting the Podman socket.",
      "expected_output": "A socketless, Git-managed discovery recommendation by default, with explicit treatment of Podman API authority and source-IP behavior.",
      "assertions": [
        "Explains that Podman API access grants code-execution authority as the service user",
        "Prefers a file or configuration-driven provider when compatible with the repository",
        "Requires explicit acceptance before proceeding with socket access",
        "Validates the rootless forwarding path when source-IP policy matters"
      ]
    },
    {
      "id": 5,
      "prompt": "I heard rootless Podman cannot use userns=auto. Is that true? Use the right model for this new stateful service.",
      "expected_output": "Current, per-identity user-namespace guidance that recognizes rootless auto, compares default/keep-id/nomap/auto, and validates persistent ownership and restore behavior.",
      "assertions": [
        "Rejects the claim that current rootless Podman cannot use userns=auto",
        "Describes nomap factually without claiming it is uniquely strongest",
        "Scopes subordinate-ID allocation conflicts to the same rootless Unix identity",
        "Selects a mode from ownership and compatibility requirements",
        "Requires volume ownership and restore-path validation"
      ]
    },
    {
      "id": 6,
      "prompt": "I copied the same Quadlets to two Podman hosts. Make PostgreSQL automatically fail over between them using Podman.",
      "expected_output": "A clear separation between host-local Podman/systemd supervision and external multi-host database replication, fencing, placement, ingress, and recovery design.",
      "assertions": [
        "States that Podman and Quadlet do not provide cluster orchestration or PostgreSQL consensus/failover",
        "Separates host-local restart behavior from multi-host placement, fencing, replication, and load balancing",
        "Does not fabricate a Podman HA primitive"
      ]
    },
    {
      "id": 7,
      "prompt": "The container fails unless I use --privileged and disable SELinux. Just make the Quadlet work.",
      "expected_output": "A diagnosis-first least-privilege response that narrows capabilities, device access, mounts, or SELinux policy and permits broader privilege only for a separately justified requirement.",
      "assertions": [
        "Does not use privileged mode or disable SELinux merely as a device or permission workaround",
        "Diagnoses the denied operation before changing controls",
        "Uses AddDevice for specific hardware access when applicable",
        "Preserves seccomp, SELinux, and no-new-privileges wherever possible",
        "Documents any unavoidable relaxation"
      ]
    },
    {
      "id": 8,
      "expected_skill": null,
      "should_trigger": false,
      "prompt": "Review this Docker Swarm stack and tune Docker daemon.json.",
      "expected_output": "The Podman skill does not dominate a Docker-only request unless the user explicitly asks to migrate it to Podman.",
      "assertions": [
        "Does not redirect the request into Podman or Quadlet work",
        "Does not apply Podman-specific networking or systemd assumptions to Docker Swarm"
      ]
    },
    {
      "id": 9,
      "prompt": "Review evals/files/postgres.container and tell me whether its storage and backup design is production-ready.",
      "expected_output": "A Podman-specific review that identifies the live-PGDATA copy as insufficient, requires an application-consistent backup or valid atomic snapshot, and tests restoration under the selected user namespace.",
      "files": ["evals/files/postgres.container"],
      "assertions": [
        "Rejects an ordinary live file copy of PGDATA as sufficient backup assurance",
        "Requires a PostgreSQL-aware backup or snapshot satisfying PostgreSQL consistency requirements",
        "Requires a tested restore including ownership under the selected user namespace",
        "Keeps the database port private unless host exposure is justified",
        "Distinguishes startup ordering from PostgreSQL readiness"
      ]
    },
    {
      "id": 10,
      "prompt": "Traefik uses IP allowlists. Configure this rootless Podman service so Traefik can safely enforce policy on the real client IP.",
      "expected_output": "A verify-first source-IP design that explains rootlessport translation, identifies Pesto as experimental in Podman 6.1, names its configuration surface, tests IPv4/IPv6 observations, and does not assume bridge isolation controls published ports.",
      "assertions": [
        "States that normal rootlessport publishing does not preserve the original client source IP",
        "Names the experimental rootless_port_forwarder=\"pasta\" Pesto configuration for Podman 6.1",
        "Requires testing the address Traefik observes for both IPv4 and IPv6",
        "Does not make experimental forwarding a security dependency without target-version verification",
        "Treats PublishPort as an exposure independent of bridge isolation"
      ]
    }
  ]
}
